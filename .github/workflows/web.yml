name: Web CI/CD Pipeline

on:
  push:
    branches:
      - main
    paths:
      - "web/**"

jobs:
  build-and-deploy-web:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push web image
        run: |
          IMAGE=dgpark3675/myweb:v${{ github.run_number }}
          docker build -t $IMAGE ./web
          docker push $IMAGE
          sed -i "s|image: .*/myweb:.*|image: $IMAGE|g" manifests/web-setting.yml
      
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Commit and push updated manifests
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add manifests/web-setting.yml
          git commit -m "Update web image tag to v${{ github.run_number }} [skip ci]" || echo "No changes to commit"
          git push
